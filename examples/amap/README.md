# AMap Layer

## CanvasLayer

将 canvas 作为二维平面图层添加到地图上

## GLCustomLayer

将 WebGL1 渲染的场景添加到地图上

（仅支持 three.js 142 版本的 WebGL1，高版本three.js采用 WebGL2）

## CustomLayer

自定义图层 (尝试加入 threejs 场景渲染，有问题)

## ImageLayer

将图片作为图层添加到地图上
